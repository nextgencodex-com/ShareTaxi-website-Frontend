export interface VehicleData {
    id: number
    name: string
    price: string
    passengers: string
    handCarry: string
    image: string
    features: string[]
    gradient: string
    buttonColor: string
  }